# D1.1 — Extension load + schema + `VectorIndex` helper

Phase **D1.1** of [`../d1-vector-build-plan.md`](../d1-vector-build-plan.md).
**Prev:** — · **Next:** [02 — D1.2a embed server](02-d1.2a-embed-server.md) · [Sequence index](README.md)

Self-contained: pure local sqlite + hand-built float vectors. No embed server, no GPU node.

```
Implement Phase D1.1 of the Blaboratory vector-retrieval system (the deferred
"D1" feature). We're on branch blaboratory-part2; sqlite-vec==0.1.9 is already
installed and committed.

Read first:
- docs/apps/blaboratory/d1-vector-build-plan.md — focus on "Decisions locked",
  "Schema (Phase 1 migration)", and "Phase D1.1". Skim topology + "How we use it"
  for context; do NOT build D1.2+ yet.
- app/apps/blaboratory/db.py — connection + PRAGMA user_version migration pattern.
- app/apps/blaboratory/event_store.py — query-helper module shape to mirror.

Build (additive only):
1. db.py: on get_connection(), enable_load_extension(True) + sqlite_vec.load(conn),
   guarded so a missing extension sets a module flag VEC_AVAILABLE=False (and logs
   once) instead of crashing. Add _migration_2 (user_version 1→2) creating the
   vec_memories vec0 vtable (embedding float[384], resident_id text, kind text,
   ref_id integer, tick integer) + the vec_rowmap table — the migration is a no-op
   when not VEC_AVAILABLE.
2. New app/apps/blaboratory/vector_index.py: is_available(); add(rows);
   query(vec, k, *, resident_id=None, kinds=None, max_chat_id=None) -> 
   [(kind, ref_id, distance)] nearest-first. Use sqlite_vec.serialize_float32.
   Scope filters in the WHERE of the MATCH/k query: resident match incl. global
   NULL (=lore) rows; kind IN (...); chat rows id <= max_chat_id (a RANGE predicate,
   honoring consumption cursors).

Done when tests/apps/test_vector_index.py passes (monkeypatch DB_PATH to tmp):
- fresh db migrates to user_version=2;
- add 3 hand-built vectors → KNN nearest-first;
- scope filters (resident_id, kind, max_chat_id range) exclude correctly;
- extension forced unavailable → migration 2 skipped, is_available() False, and
  existing event/chat stores still work.
Also run the full existing suite (the migration touches shared db.py).

Env: .venv/bin/python, .venv/bin/pytest (asyncio_mode=auto), py_compile.
Don't commit until I've reviewed.
```
