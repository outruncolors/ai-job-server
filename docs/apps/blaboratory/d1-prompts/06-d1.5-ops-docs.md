# D1.5 — Ops, docs, polish + real end-to-end

Phase **D1.5** of [`../d1-vector-build-plan.md`](../d1-vector-build-plan.md).
**Prev:** [05 — D1.4 hybrid retrieval](05-d1.4-hybrid-retrieval.md) · **Next:** — · [Sequence index](README.md)

The first phase that needs the **real** embed server on the deployed secondary.

```
Implement Phase D1.5 of the Blaboratory vector-retrieval system (ops + docs +
real end-to-end). Branch blaboratory-part2. All code phases (D1.1–D1.4) are in.

Read first:
- docs/apps/blaboratory/d1-vector-build-plan.md — "Phase D1.5", "Operator
  prerequisites", "Risks & open questions".
- docs/multi-machine.md — the deploy flow (bare repo, systemd user unit, cutover).
- docs/apps/blaboratory/index.md and design.md "Part 2 — Build notes".

Do:
1. Deploy checklist doc (new docs/apps/blaboratory/ ops note OR a section in
   index.md): the bge-small GGUF must be at /opt/ai-stack/models/ on the secondary;
   the deployed config must carry embed_port/embed_model_path; the embed manager
   starts at lifespan on the llm node; the index + KNN run only on the web node.
2. design.md: add a "D1 — what landed / drifted" subsection (mirror the Part 1/2
   build-notes style) — record bge-small/384-dim, hybrid retrieval, app-managed
   embed server, recency fallback, and any drift from this plan.
3. Optional: a Server → LLM UI tile showing embed-server status + start/stop
   (reuse the chat llama.cpp tile pattern in static/server/llm-models-tab.js).
4. Re-index note: switching the embedder = DROP vec_memories + clear vec_rowmap +
   re-run index_pending (different dim = incompatible vtable). State it loudly.

Real end-to-end verification (record results):
- Full suite green: .venv/bin/pytest -q.
- On the deployed setup: fire ticks; seed a distinctive earlier chat, then a
  related moment many ticks later; confirm via GET /residents/{id}/context that
  the older-but-relevant memory resurfaces in [You Know] (it wouldn't under pure
  recency). Confirm the sim still runs if the embed server is stopped (degrades to
  recency, logs once).

Env: .venv/bin/python, .venv/bin/pytest (asyncio_mode=auto), py_compile.
Don't commit until I've reviewed.
```
