# D1.2b — `embed()` client + embedding config (web node)

Phase **D1.2b** of [`../d1-vector-build-plan.md`](../d1-vector-build-plan.md).
**Prev:** [02 — D1.2a embed server](02-d1.2a-embed-server.md) · **Next:** [04 — D1.3 indexing](04-d1.3-indexing.md) · [Sequence index](README.md)

Depends on D1.2a's config fields. Testable with a mocked httpx transport (no live server).

```
Implement Phase D1.2b of the Blaboratory vector-retrieval system. Branch
blaboratory-part2. Goal: an async embed() that turns text into vectors by calling
the embed llama-server (port 8081 on the llm node) built in D1.2a.

Read first:
- docs/apps/blaboratory/d1-vector-build-plan.md — "Phase D1.2b" + "How we use it"
  (query/document asymmetry: bge prepends a query-only instruction prefix; stored
  docs are embedded raw).
- app/chain/llm_client.py — OpenAICompatibleLLMClient; mirror its httpx error
  mapping (ConnectError/TimeoutException/HTTPStatusError → clear RuntimeError).
- app/chain/llm_swap.py — resolve_llm_server_url / find_peer_for_capability("llm")
  peer-discovery pattern (host); reuse it, swap the port for the embed port.
- The embed config from D1.2a (embed_port etc.).

Build (additive):
1. OpenAICompatibleLLMClient.embed(texts: list[str], *, is_query: bool=False)
   -> list[list[float]]: POST {api_base}/embeddings with {"input": texts,
   "model": ...}; batch input in one call; return vectors in input order; map
   errors to a clear EmbedError.
2. app/apps/blaboratory/embeddings.py: config loader (port/model/dim=384/
   query_prefix) + embed_url() (host from the llm peer, embed port) +
   embed_texts(texts, *, is_query) that applies query_prefix only when is_query.

Done when tests/apps/test_embeddings.py passes (httpx MockTransport):
- batches N texts → N vectors of length dim;
- query prefix applied iff is_query=True (assert on the captured request body);
- connect/timeout/HTTP-error → EmbedError.
Full existing suite stays green.

Env: .venv/bin/python, .venv/bin/pytest (asyncio_mode=auto), py_compile.
Don't commit until I've reviewed.
```
